[phases.setup]
nixPkgs = ["python311", "ffmpeg", "git", "gcc", "openssl"]

[phases.install]
cmds = [
    "python3 -m venv /opt/venv",
    "/opt/venv/bin/pip install --upgrade pip",
    "/opt/venv/bin/pip install -r requirements.txt"
]

[start]
cmd = "/opt/venv/bin/python3 -m TEAMXMUSIC"
